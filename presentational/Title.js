import styled from "styled-components"

import {grey, normal, medium} from "./definitions"

export const Title = styled.h2`
  margin: 0 0 6px 0;

  font-size: ${medium};
  font-weight: ${normal};
  line-height: 1.1em;
  color: ${grey};
`
