import styled from "styled-components"

import {primary, bold, extraLarge} from "./definitions"

export const SiteTitle = styled.h2`
  margin-bottom: 64px;

  font-size: ${extraLarge};
  font-weight: ${bold};
  line-height: 1.1em;
  color: ${primary};
`
